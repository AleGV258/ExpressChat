<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/Style.css">
    <title>Agregar Usuarios</title>
</head>
    <% if(typeof users === 'undefined'){ %>
        
    <% }else{ %>          
        <div id="scroll2">
            <% for( user of users) { %>
                <div id="">
                    Nombre: <%= user.Nombre %> <br>
                    Email: <%= user.Correo %>
                    <div id="usuarios" name="<%= chat.IdChat %>" onclick="window.location.href = 'agregarUsuario/<%= IdChat %>/<%= user.IdUsuario %>'">
                        Agregar
                    </div>
                </div>
            <% }; %>
        </div>
    <% } %>
<body>

</body>

</html>